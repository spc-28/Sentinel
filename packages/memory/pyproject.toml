[project]
name = "sentinel-memory"
version = "0.1.0"
description = "Sentinel memory — learn from past incidents (fingerprint, recall, patterns)."
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.7.0",
    "structlog>=24.1.0",
    "sqlalchemy[asyncio]>=2.0.30",
]
