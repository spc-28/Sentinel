[project]
name = "sentinel-tools"
version = "0.1.0"
description = "Sentinel tool access layer — data-fetching functions for the agents."
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.7.0",
    "structlog>=24.1.0",
    "numpy>=1.26.0",
    "pandas>=2.2.0",
    "prophet>=1.1.5",
    "httpx>=0.27.0",
]
