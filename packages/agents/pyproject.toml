[project]
name = "sentinel-agents"
version = "0.1.0"
description = "Sentinel investigation agents (LangChain + LiteLLM)."
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.7.0",
    "structlog>=24.1.0",
    "langchain>=0.3.0",
    "langchain-litellm>=0.2.0",
    "litellm>=1.55.0",
]
