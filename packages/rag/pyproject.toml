[project]
name = "sentinel-rag"
version = "0.1.0"
description = "Sentinel RAG — search over runbooks and logs (Qdrant + embeddings + NLI)."
requires-python = ">=3.13"
dependencies = [
    "sentence-transformers>=3.0.0",
    "qdrant-client>=1.10.0",
    "rank-bm25>=0.2.2",
    "numpy>=1.26.0",
    "structlog>=24.1.0",
]
