[project]
name = "sentinel-mcp-sentinel"
version = "0.1.0"
description = "The one MCP server: exposes Sentinel itself to AI clients (Claude Desktop, Cursor)."
requires-python = ">=3.13"
dependencies = [
    "mcp>=1.2.0",
    "httpx>=0.27.0",
    "uvicorn>=0.30.0",
    "structlog>=24.1.0",
]
