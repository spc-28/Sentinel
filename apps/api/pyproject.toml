[project]
name = "sentinel-api"
version = "0.1.0"
description = "Sentinel API — receives alerts and serves investigation data."
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.111.0",
    "uvicorn[standard]>=0.30.0",
]
